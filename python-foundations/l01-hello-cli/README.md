# Hello-cli

This is a simple cli util, written in ```Python```. It takes name and languages names as arguments and prints hello messages on specified languages

## Usage

```bash
hello-cli NAME [-l]...
```
